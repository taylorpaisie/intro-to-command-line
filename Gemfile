source "https://rubygems.org"

# Use the same dependency set GitHub Pages uses for Jekyll builds.
# This helps keep local/CI builds consistent with Pages.
gem "github-pages", group: :jekyll_plugins

# Required for Ruby 3+ when running `jekyll serve` locally.
gem "webrick"
